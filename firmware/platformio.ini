; AirPlay Album Art display firmware — see ../design.md for the full system design.
;
; env:tft-preview  — interim Adafruit ESP32-S3 Reverse TFT Feather (letterboxed
;                    64x64 preview on the built-in 240x135 ST7789).
; A `hub75` environment will be added when the 64x64 P3 matrix arrives; only the
; output driver changes (build flag), never the rendering.

[env:tft-preview]
platform = espressif32
board = adafruit_feather_esp32s3_reversetft
framework = arduino
monitor_speed = 115200
build_flags =
    -DOUTPUT_TFT_PREVIEW
lib_deps =
    adafruit/Adafruit GFX Library@^1.11.11
    adafruit/Adafruit ST7735 and ST7789 Library@^1.10.4
    links2004/WebSockets@^2.6.1
    bblanchon/ArduinoJson@^7.4.2
